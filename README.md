# My-first-python-program-
# to check if the person is allowed to vote 

name = input(" enter the name of person: ")

age = int(input( " enter the age of person: "))

If( age >= 18 ):
Print( name, " is allowed to vote")

else:
Print( name, " is not allowed to vote")
